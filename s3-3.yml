Parameters:
  BucketName:
    Type: String
    Description: Enter the name of the bucket

Resources:
  MyProdS3Bucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref BucketName
